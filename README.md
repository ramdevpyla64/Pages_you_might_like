# Pages_you_might_like
A simple Python project that recommends new pages a user might like based on shared liked pages among users. The system reads user and page data from a JSON file, computes shared interests, and suggests relevant pages using similarity scoring.
